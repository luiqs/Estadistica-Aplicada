### 4. Paquete extra: DataExplorer

#Aun en su version 0.8.2, el paquete "DataExplorer" es un nuevo 
#paquete que espera ver su version final el 2021 que nos permite
#obtener un resumen de varios atributos de la base de datos. 
#Los gráficos que genera, son basados en el lenguaje ggplot2 
#(aunque estos se corren automaticamente, sino que nosotros podamos
#editar o cambiar facilmente los colores y otros atributos). 
#Este paquete nos permite ver rapidamente nuestra data y tener 
#una claridad de que esta ocurriendo con las variables en nuestra 
#base de datos. Instalemoslo como siempre y luego activemoslo:
  

























